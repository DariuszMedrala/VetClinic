# Obraz bazy danych — wersja przypięta dla powtarzalności builda.
# Dane logowania (POSTGRES_USER/PASSWORD/DB) wstrzykiwane są z .env
# przez docker-compose, a NIE zaszyte w obrazie.
FROM postgres:16-alpine
